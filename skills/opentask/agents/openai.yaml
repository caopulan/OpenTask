interface:
  display_name: "OpenTask"
  short_description: "Run root-session OpenTask workflows"
  default_prompt: "Use $opentask to turn this OpenClaw conversation into a root-session workflow and keep it running through files, subagents, and cron."
